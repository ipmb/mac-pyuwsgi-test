sudo: required

services:
    - docker

before_install:
    - docker build -t pyuwsgi_wheel .
    - docker build --build-arg="NO_BINARY=pyuwsgi" -t pyuwsgi_sdist .

script: |
    echo "Build from manylinux wheel:"; docker run pyuwsgi_wheel;
    echo "Build from sdist"; docker run pyuwsgi_sdist


